---
import type { HTMLAttributes } from "astro/types";

interface Link {
    name: string;
    href: string | null;
    target?: HTMLAttributes<"a">["target"];
}
export interface Props {
    headings: Link[];
    headingStyle: string;
}

const { headings, headingStyle } = Astro.props;
---

<nav class="bg-accent text-background mt-16 p-8 sm:px-16 sm:py-4 xl:mt-0">
    <div
        class="mx-auto flex w-full max-w-screen-xl flex-col gap-x-16 gap-y-4 sm:flex-row sm:flex-wrap sm:justify-between"
    >
        {
            headings.map(({ name, href, target }) => {
                return (
                    <a
                        href={href}
                        aria-disabled={href === null}
                        class:list={[
                            headingStyle.replace("mb-4", ""),
                            "decoration-green-900 text-shadow-2xs text-shadow-green-900",
                            href === null && "cursor-not-allowed opacity-60",
                        ]}
                        target={target}
                        data-highlight
                    >
                        {name}
                    </a>
                );
            })
        }
    </div>
</nav>
